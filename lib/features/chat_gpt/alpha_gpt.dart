import 'package:flutter/material.dart';

class Gpt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gpt'),
      ),
      body: Center(
        child: Text('Welcome to AI'),
      ),
    );
  }
}